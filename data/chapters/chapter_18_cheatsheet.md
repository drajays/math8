# Cheat Sheet — Chapter 18: Visualising Solid Shapes

| # | Fact / Formula | Note ID |
|---|---|---|
| 1 | A sketch of 3-D shape in which the measurements of the sides are proportional to their actual length | `CH18-sec-isometric-sketches` |
| 2 | A solid made up of polygonal regions is called polyhedron. | `CH18-sec-polyhedron` |
| 3 | Polyhedrons that are made up of convex polygons are called convex polyhedrons i.e. polyhedron whose  | `CH18-sec-convex-polyhedrons` |
| 4 | A polyhedron whose base and top are congruent polygons and whose lateral faces are parallelograms in | `CH18-sec-prism` |
| 5 | A prism is named after its base, if base is a triangle then it is called a triangular prism. Various | `CH18-sec-prism` |
| 6 | A prism whose base and top are congruent triangles and lateral faces are rectangles is called a tria | `CH18-sec-triangular-prism` |
| 7 | A prism whose base and top are congruent rectangles and lateral faces are rectangles is called a rec | `CH18-sec-rectangular-prism` |
| 8 | A prism whose base and top are congruent pentagons and lateral faces are rectangles is called a pent | `CH18-sec-pentagonal-prism` |
| 9 | A prism whose base and top are congruent hexagons and lateral faces are rectangles is called a hexag | `CH18-sec-hexagonal-prism` |
| 10 | A polyhedron whose base is a polygon (of any number of sides) and whose lateral faces are triangles  | `CH18-sec-pyramid` |
| 11 | A pyramid whose base is a triangle is called a triangular pyramid. It is also known as tetrahedron. | `CH18-sec-triangular-pyramid` |
| 12 | A pyramid whose base is a rectangle is called a rectangular pyramid. | `CH18-sec-rectangular-pyramid` |
| 13 | A pyramid whose base is a square is called a square pyramid. | `CH18-sec-rectangular-pyramid` |
| 14 | A pyramid whose base is a pentagon is called a pentagonal pyramid. | `CH18-sec-square-pyramid` |
| 15 | A pyramid whose base is a hexagon is called a hexagonal pyramid. | `CH18-sec-hexagonal-pyramid` |
| 16 | Example 2. Using Euler's formula find the unknown: | `CH18-sec-note` |
| 17 | 3. Verify Euler's formula for these solids: | `CH18-sec-exercise-18-2` |
| 18 | Solution. Using Euler's formula: $ F + V = E + 2 $ | `CH18-sec-mcq-solved` |
| 19 | (i) A solid made up of polygonal regions is called ... | `CH18-sec-mental-maths` |
| 20 | (iv) A polyhedron whose base and top are congruent polygons and whose lateral faces are parallelogra | `CH18-sec-mental-maths` |
