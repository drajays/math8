# Cheat Sheet — Chapter 20: Data Handling

| # | Fact / Formula | Note ID |
|---|---|---|
| 1 | The data arranged in this form is called data array or arrayed data. The presentation of the data in | `CH20-sec-data-handling` |
| 2 | An action which results in some (well defined) outcomes is called an experiment. | `CH20-sec-represent-the-above-data-by-a-double-bar-graph` |
| 3 | An experiment is called random if it has more than one possible outcomes and it is not possible to t | `CH20-sec-represent-the-above-data-by-a-double-bar-graph` |
| 4 | In such cases, we may say that the different outcomes of the experiment are equally likely. This mea | `CH20-sec-represent-the-above-data-by-a-double-bar-graph` |
| 5 | An event which always happens is called a sure event or a certain event. | `CH20-sec-represent-the-above-data-by-a-double-bar-graph` |
| 6 | An event which never happens is called an impossible event. | `CH20-sec-represent-the-above-data-by-a-double-bar-graph` |
| 7 | 50, the total runner is getting at least one head, means getting one head or two heads and the outco | `CH20-sec-represent-the-above-data-by-a-double-bar-graph` |
| 8 | 2. Since there is a kink from 0 to 50, it means that no student scored less than 50. | `CH20-sec-t-white` |
| 9 | (ii) In a grouped frequency distribution, the difference between lower limit and upper limit a class | `CH20-sec-t-white` |
| 10 | (iii) The mid point of the class interval is called | `CH20-sec-t-white` |
| 11 | Data mostly available to us in an unorganized form is called raw data. | `CH20-sec-summary` |
| 12 | ★ The mid point of the class interval is called class mark. | `CH20-sec-summary` |
| 13 | An event which always happens is called a sure event. $P(\text{sure event}) = 1$ | `CH20-sec-likely` |
| 14 | An event which never happens is called an impossible event. | `CH20-sec-likely` |
| 15 | 13. Verify Euler's formula for decagonal pyramid. | `CH20-sec-likely` |
